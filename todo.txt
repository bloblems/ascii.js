- draw_layer() should be able to draw only a part of the layer (with width and
  height)

- draw() updates only the displayed the <span> elements (optimization)

- create_style_layer();
- set_style();
