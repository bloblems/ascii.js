- draw() updates only the displayed the <span> elements (optimization)

- create_style_layer()
- set_style()

- text() new mode TEXT_BLOCK which handles '\n' and white spaces to put predrawn
  strings.
